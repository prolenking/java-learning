server:
  port: 8080

spring:
  session:
    store-type: none
  datasource:
    name: test
    url: jdbc:mysql://127.0.0.1:3306/bookdb?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver




mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
  type-aliases-package: com.javalearning.entity
  global-config:
    refresh-mapper: true
  
