<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="javalearning.javalearning.mapper.BookMapper">
    <resultMap id="book" type="javalearning.javalearning.entity.ShopBook">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="author" property="author"/>
        <result column="isbn" property="isbn"/>
        <result column="place_of_publication" property="placeOfPublication"/>
        <result column="publication_date" property="publicationDate"/>
        <result column="press" property="press"/>
    </resultMap>
    <select id="selectBookById" parameterType="java.lang.Long" resultMap="book">
        select * from shop_book where id = #{id}
    </select>

</mapper>